# DateMath

DateMath is a PHP class which makes date manipulation simple.

## Usage

$myDate = new DateMath('2011-01-01'); 

$myDate->addMonths(2)->addDays(3)->firstDayOfWeek();

echo $myDate->toString('Y-m-d');
